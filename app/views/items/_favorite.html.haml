%i.fa.fa-ster
- if user_signed_in?
  - if @item.favorited_by?(current_user)
    / ログインしているユーザーがファボしたかどうかで分岐
    = icon('fa','star',class: 'icon')
    %span お気に入り登録数:
    = link_to @item.favorites.count, item_favorites_path(@item.id), method: :delete
  - else
    = icon('fa','star',class: 'icon')
    %span お気に入り登録数:
    = link_to @item.favorites.count, item_favorites_path(@item.id), method: :post
- else
  %p
    %span お気に入り数:
    = @item.favorites.count